<com.mytv365.zb.pullableview.PullToRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/refresh_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <include layout="@layout/refresh_head" />
    <!-- 支持所有实现Pullable接口的View -->

    <com.mytv365.zb.pullableview.PullableTextView
        android:id="@+id/content_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        android:gravity="center"
        android:text="这是一个match_parent的TextView"
        android:textColor="#ffffff"
        android:textSize="40sp"
        android:textStyle="bold" />

    <include layout="@layout/load_more" />

</com.mytv365.zb.pullableview.PullToRefreshLayout>